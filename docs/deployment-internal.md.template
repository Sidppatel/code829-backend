# Deployment Internals (Template)

> This file is the template for `docs/deployment-internal.md`, which is **gitignored**
> and holds concrete values that must not land in source control. Copy it to
> `deployment-internal.md` and fill in from the provider dashboards. Rotate the
> moment it touches a chat thread or a commit diff.

## Supabase

- **Project ref:** `<ref>` (from dashboard URL `app.supabase.com/project/<ref>`)
- **Region:** `<e.g. aws-us-east-1>`
- **Session connection string:** `postgresql://<user>:<password>@<host>:5432/postgres`
- **Transaction pooler string:** `postgresql://<user>:<password>@<host>:6543/postgres?pgbouncer=true`
- **Password owner:** `<on-call>` — rotation cadence 90 days

## Upstash Redis

- **Endpoint:** `<host>.upstash.io:6380`
- **TLS URL:** `rediss://default:<password>@<host>.upstash.io:6380`
- **Region:** `<e.g. us-east-1>`

## Render

- **Service ID:** `srv-<id>`
- **Deploy hook URL:** `<keep out of chat — dashboard-only>`
- **Primary env vars:** see `render.yaml`; secrets are dashboard-only

## Cloudflare

- **Account ID:** `<id>`
- **API token owner:** `<on-call>` — scope: Workers Scripts Edit + Account Read + Zone Read

## Change log

| Date | Change | Operator |
|---|---|---|
| YYYY-MM-DD | Initial populate | `<name>` |
